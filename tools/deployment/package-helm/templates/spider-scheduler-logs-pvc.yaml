{{- if eq .Values.clpConfig.compression_scheduler.type "spider" }}
{{- include "clp.createPvc" (dict
  "root" .
  "component" "spider-scheduler-logs"
  "capacity" "5Gi"
  "accessModes" (list "ReadWriteOnce")
) }}
{{- end }}
